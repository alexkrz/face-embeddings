channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11.*
  - pytorch=2.4.* # =*cuda* # Need to specifically request cuda version
  - torchvision=0.19.*
  - ipykernel=6.29.*
  - pip>=23
  # Install pip requirements with `pip install -r requirements.txt`
